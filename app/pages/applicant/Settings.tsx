import AccountSettingsPage from '../AccountSettingsPage';

/** Applicant dashboard → Settings uses shared account shell */
export default function Settings() {
  return <AccountSettingsPage layoutRole="applicant" />;
}
